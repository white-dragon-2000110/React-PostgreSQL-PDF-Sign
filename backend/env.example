PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=signature_user
DB_PASSWORD=yourpassword
DB_NAME=signature_app

# Relative or absolute path
UPLOAD_DIR=uploads

# Template with placeholders {input} and {output}
SIGN_COMMAND_TEMPLATE=libresign --sign "{input}" --output "{output}"